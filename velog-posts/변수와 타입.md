<h4 id="변수-선언">변수 선언</h4>
<pre><code>int age;       // 정수형 타입(int) 
double value;  // 실수형 타입(double)</code></pre><h4 id="값-저장">값 저장</h4>
<pre><code>int score;     // 변수 선언
score = 90;    // 값 저장

int score = 90;</code></pre><h4 id="변수-사용">변수 사용</h4>
<pre><code>int hour = 3;
int minute = 5;

System.out.println(hour + &quot;시간&quot; + minute + &quot;분&quot;)</code></pre><blockquote>
<p>변수는 또 다른 변수에 대입하여 값을 복사할 수 있다. </p>
</blockquote>
<pre><code>int temp = x;
x = y;
y = temp;</code></pre><blockquote>
<p>정수 타입 : byte, char, short, int, long
실수 타입 : float, double
논리 타입 : boolean</p>
</blockquote>
<h4 id="이스케이프-문자">이스케이프 문자</h4>
<pre><code>String str = &quot;나는 \&quot;자바\&quot;를 좋아합니다.&quot;;
System.out.println(str);</code></pre><h4 id="문자열을-기본-타입으로-변환">문자열을 기본 타입으로 변환</h4>
<pre><code>String str = &quot;3000&quot;;
int value = Integer.parseInt(Str);</code></pre><h4 id="기본-타입을-문자열로-변환">기본 타입을 문자열로 변환</h4>
<pre><code>String str = String.valueof(기본타입값);</code></pre>